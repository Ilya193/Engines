<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Engines" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <style name="Theme.Engines" parent="Base.Theme.Engines" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/windowBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/construction_black</item>
        <item name="postSplashScreenTheme">@style/Theme.Engines</item>
    </style>
</resources>